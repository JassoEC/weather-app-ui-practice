import {BaseTextProps, BoldBaseText} from '../BaseText';

interface Props extends BaseTextProps {}

export const DefBoldCaption1 = ({
  text,
  opacity,
  color,
  fontSize = 10,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldCaption2 = ({
  text,
  opacity,
  color,
  fontSize = 8,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldSubHeadline = ({
  text,
  opacity,
  color,
  fontSize = 12,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldBody = ({
  text,
  opacity,
  color,
  fontSize = 13,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldHeadLIne = ({
  text,
  opacity,
  color,
  fontSize = 14,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldTitle3 = ({
  text,
  opacity,
  color,
  fontSize = 15,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldTitle2 = ({
  text,
  opacity,
  color,
  fontSize = 24,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldTitle = ({
  text,
  opacity,
  color,
  fontSize = 20,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefBoldLargeTitle = ({
  text,
  opacity,
  color,
  fontSize = 24,
  style,
}: Props) => {
  return (
    <BoldBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};
