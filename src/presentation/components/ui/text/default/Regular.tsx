import {BaseTextProps, RegularBaseText} from '../BaseText';

interface Props extends BaseTextProps {}

export const DefRegularCaption1 = ({
  text,
  opacity,
  color,
  fontSize = 10,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularCaption2 = ({
  text,
  opacity,
  color,
  fontSize = 8,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularSubHeadline = ({
  text,
  opacity,
  color,
  fontSize = 12,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularCallout = ({
  text,
  opacity,
  color,
  fontSize = 13,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularBody = ({
  text,
  opacity,
  color,
  fontSize = 14,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularHeadLIne = ({
  text,
  opacity,
  color,
  fontSize = 15,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularTitle3 = ({
  text,
  opacity,
  color,
  fontSize = 16,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularTitle2 = ({
  text,
  opacity,
  color,
  fontSize = 18,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularTitle = ({
  text,
  opacity,
  color,
  fontSize = 34,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};

export const DefRegularLargeTitle = ({
  text,
  opacity,
  color,
  fontSize = 44,
  style,
}: Props) => {
  return (
    <RegularBaseText
      text={text}
      opacity={opacity}
      color={color}
      fontSize={fontSize}
      style={style}
    />
  );
};
